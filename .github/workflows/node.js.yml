name: Server CI

on:
  push:
    branches: [ "main" ]
    paths:
      - "server/**"

jobs:
  build:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: '_work/mf-deployment/mf-deployment/server/package-lock.json'

      # Copy server folder (actually no need — repo already contains it)
      - name: Prepare server workspace
        run: |
          mkdir -p ./server
          cp -R ./_work/mf-deployment/mf-deployment/server ./server-temp
          mv ./server-temp/server ./server  

      - name: Install server dependencies
        run: |
          cd server
          npm ci

      - name: Injecting Secrets
        run: |
          touch server/.env
          echo ${{ secrets.PROD_ENV_SECRETS }} > server/.env
